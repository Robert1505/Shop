import React, { ReactElement } from 'react'

interface Props {
    
}

export default function FavouritesPage({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}
